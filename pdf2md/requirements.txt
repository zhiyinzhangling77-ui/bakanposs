# PDF → Markdown 変換パイプラインの依存関係
# 注: marker-pdf / mineru は torch を含む大きなモデル依存を引き込みます。
#     GPU 環境では CUDA 対応の torch を先に入れると速いです(README 参照)。

marker-pdf>=1.0.0     # 主バックエンド(レイアウト/数式/表に強い)
mineru>=2.0.0         # フォールバック(日本語・中国語に強い)
PyMuPDF>=1.24.0       # 目次(埋め込みアウトライン)抽出
anthropic>=0.40.0     # 各章の Claude 要約(3行要約+重要語10個)
