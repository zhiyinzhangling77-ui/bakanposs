# 解析A 一括 Python 環境  (v4 / v9 / v10 / v11 共通)
# ----------------------------------------------------------
# 推奨: Python 3.10–3.12
#
# 仮想環境作成例 (venv):
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# conda の場合:
#   conda create -n analysisA python=3.11 -y
#   conda activate analysisA
#   pip install -r requirements.txt
#
# 含まれるもの:
#   - 数値計算/可視化           (numpy, pandas, scipy, matplotlib)
#   - NetCDF/ERA5/GRACE 読込    (xarray, netCDF4, h5netcdf, dask, bottleneck)
#   - Excel 読込 (v4 が xlsx)   (openpyxl)
#   - Parquet 中間ファイル      (pyarrow ; v4 → v11 連携)

# --- 数値計算 / 可視化 ---
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
matplotlib>=3.7,<4.0

# --- NetCDF / GRACE / ERA5 読み込み ---
xarray>=2024.1
netCDF4>=1.6.5
h5netcdf>=1.3
dask>=2024.1
bottleneck>=1.3.7

# --- 入出力 ---
openpyxl>=3.1      # v4: Oran/Tarazona の .xlsx 読込
pyarrow>=15.0      # v4 → v11 中間ファイル(parquet)
