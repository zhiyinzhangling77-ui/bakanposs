# HUMAN_GATES — 人手が要る箇所の台帳

**初版**: 2026-09-01 / 由来: `SESSION_STATE.md`(旗99 時点)・`FLAGS_LOG.md`(旗102)・`OPEN_QUESTIONS_OPTIONS.md`・
`japanflux_pn/RUN_HEAVY.md`・`LITERATURE_VERIFICATION_TODO.md`・`CONTACT_DRAFT_TKY.md`・`NEW_OBSERVATION_DESIGN.md` からの静的抽出
**更新**: `research/GATE_AUDIT_PROMPT.md` を回して差分更新（既存 ID は維持）
**status**: `open` / `resolved` / `wont-do`

分類: **D**=ローカル実行 / **A**=取得・認証 / **P**=外界への問い合わせ / **J**=研究判断 / **S**=外部仕様

> ⚠ 本台帳は静的抽出であり、**旗102 以降の状況は反映されていない**。
> 実セッションで `GATE_AUDIT_PROMPT.md` を回して検証すること。

---

## 依存ランキング（解けば最も多くが動く順）

1. **GATE-05（AmeriFlux 未取得 26 組）** — 旗85 で**名指しまで済んでいる**。
   ★で選ばずに同一地点の証拠を**約 2 倍**にでき、**★選択バイアスの訂正を証拠で裏打ちできる**。
   直近の旗が ▲ を積み続けている中で、**唯一「陽性が増える可能性がある」手**。
2. **GATE-01（ローカル実行の一括セッション）** — D 系すべての根。手 C・手 B・RUN_HEAVY ①〜④ が一斉に動く。
3. **GATE-11（卒論のスコープ確定）** — どこで打ち止めるかが決まらないと、
   手 F（文献依存を減らす書き換え）も新規観測への引き渡しも書けない。S 系 2 件も同時に解ける。

```
GATE-17 (旗103 の道具を合成で走らせる・2 分・データ不要) ──→ GATE-02
GATE-01 (ローカル実行セッション)
  ├─ GATE-02 (手C 降雨からの経過・まず下調べ) ──→ Bowen 反転の未解明部分
  ├─ GATE-03 (手B 変化の向き)          ──→ 同上
  ├─ GATE-04 (RUN_HEAVY ①〜④)         ──→ FUTURE_PLAN Phase 1/2 の頑健性
  └─ GATE-06 (PhenoCam 取得物の再解析)  ──→ 旗94/95 の積み残し

GATE-05 (AmeriFlux 26 組) ──→ 旗85 の訂正を証拠で裏打ち ──→ GATE-10 (公表物の反映判断)
GATE-07 (一次文献 3 件)   ──→ 旗92 の宙吊りを解消 ──→ 手F
GATE-08 (COSORE 多層)     ──→ 軸B の兆し（旗102）… ただし下限未満で**望み薄**

GATE-11 (卒論スコープ)
  ├─ GATE-14 (提出形式・締切・字数)
  └─ GATE-15 (synthesis.html の更新可否)
```

---

## バッチ化（あなたが席に着く回数を最小化する）

| バッチ | 内容 | GATE | 想定 |
|---|---|---|---|
| **B1: 夜間放置 1 回** | RUN_HEAVY ①〜④ を `nohup` で仕掛けて寝る | 04 | 仕掛け 10 min + 夜通し |
| **B2: ローカル解析 1 回** | **旗103 の合成検証**（2 min・**データ不要なので最初に**）→ 手C の下調べ・手B、PhenoCam 再解析 | **17**, 02, 03, 06 | 1–2 h |
| **B3: 取得（待ち時間が長い→最初に着手）** | AmeriFlux 26 組・COSORE 追加 | 05, 08 | 30 min + 審査待ち |
| **B4: egress のある環境か図書館** | 一次文献 3 件の確認 | 07 | 1 h |
| **B5: 判断のみ（PC もデータも不要）** | スコープ・訂正の反映・兆しの扱い・下限の遡及 | 10, 11, 12, 13 | 30 min・会話だけで可 |
| **B6: 送って待つ** | サイト管理者への連絡（必要なら） | 09 | 10 min + 返信待ち |

> **B5 は今この場で片付く。** ループが止まった直後に答えれば自走可能な範囲が一気に広がる。
> **B3 と B4 は待ち時間が支配的**なので、**最初に着手**すると全体が縮む。

---

## D — ローカル実行（`/mnt/hdd` はコンテナ外）

> **★2026-09-02（旗104）の重要な訂正**：**この台帳は「D 分類は必ず人間待ち」を前提に書かれているが、
> それは環境による。** **`ls /mnt/hdd` が通る機械では Python も git も動き、D 分類は
> ゲートではない**——**周を回している主体が自分で実行してよい**。
> **旗104 は実際に GATE-17 を自分で消化した。**
> **周の最初に必ず環境を確かめること**：
> ```bash
> ls /mnt/hdd/ 2>/dev/null && echo "LOCAL: 実データあり" || echo "CONTAINER: 実データなし"
> ```
> **D 分類がゲートになるのはコンテナ側の周だけである。**

### GATE-01 — ローカル実行セッションの確保
- **status**: open（常設ゲート）
- **なぜ私にできないか**: 実データは `/mnt/hdd/...` にあり、コンテナからネットワーク的にも権限的にも到達しない。
  これは `SESSION_STATE.md` 冒頭に既に明記された運用である。
- **あなたが取る行動**: リポジトリ直下で最新を引き、下の GATE のコマンドを流す。
  ```bash
  cd ~/bakanposs && git pull origin claude/new-branch-creation-heq0i1
  python -m pytest tests/ -q
  ```
- **返すもの**: pytest の結果（**道具が壊れていないことの確認**が先）
- **見積り**: 10 min / **迂回**: 不可（D 系の根）

### GATE-18 — 旗103 の**未コミットの作業をコミットして push する**（★最優先・1 分）
- **status**: **resolved（2026-09-02・旗104 の周）** — `33f9271` で 5 ファイルとも入っている。
  **以下は経緯の記録として残す。**
- ~~**status**: open~~ / **依存**: なし
- **なぜ私にできないか**: **この環境では `git add` が承認待ちになる**（非対話セッションでは
  承認を得られない）。**Python 実行と同じ権限の境界であり、能力の問題ではない。**
- **あなたが打つコマンド**:
  ```bash
  cd ~/bakanposs
  git add research/rain_history_probe_step103.py research/FLAGS_LOG.md \
          research/SESSION_STATE.md research/HUMAN_GATES.md research/LOOP_PROTOCOL.md
  git status --short --untracked-files=no      # ★ この 5 つだけか確認してから
  git commit -m "旗103 手C の前提の下調べ道具（未検証）と、コンテナでは合成が走らない事実"
  git push -u origin "$(git rev-parse --abbrev-ref HEAD)"
  ```
- **返すもの**: なし（**push できたことだけ**）
- **解けると動くもの**: **`run_loop.sh` は追跡ファイルが汚れていると止まる**——
  **これを消化するまで次の自走周が動かない。**
- **見積り**: 1 min / **迂回**: 不可

### GATE-17 — 旗103 の道具を**合成データで走らせる**（★これが今いちばん安い一手）
- **status**: **resolved（2026-09-02・旗104）** — **実データのある機械で回った周が自分で消化した。**
  **結果**：自己試験は通過。**しかし `separable`（陽性の対照）が期待どおりに出なかった**——
  **`遠い`(≥7 日) が両季節とも下限未満**で、**「下限を満たす」と印字する枝に一度も到達していなかった**。
  **道具の欠陥 #36 として記録し、合成の生成側だけを較正して四つとも区別できるようにした**
  （**判定規則・しきい値は一つも変えていない**）。詳細は `FLAGS_LOG.md:4257`。
- ~~**status**: open~~ / **依存**: なし（**実データも要らない**）
- **なぜ私にできないか**: **このコンテナでは Python を実行できない**（旗103）。
  `python3 --version` は通るが、**スクリプト実行と `python3 -c` は承認待ちになり、
  非対話セッションでは承認を得られない**。**能力ではなく権限の境界である。**
- **あなたが打つコマンド**:
  ```bash
  cd ~/bakanposs && git pull origin claude/new-branch-creation-heq0i1
  python research/rain_history_probe_step103.py --check     # 自己試験だけ（数秒）
  python research/rain_history_probe_step103.py             # 合成 4 ケース（1 分未満）
  ```
- **返すもの**: **出力ログ全文**（**自己試験が「通過」か**、および
  **`separable`/`confounded`/`sparse`/`nodata` の 4 ケースが期待どおり分かれたか**）。
  **例外で落ちたらトレースバックをそのまま**（**書いたが一度も動かしていない道具である**）
- **解けると動くもの**: GATE-02（実データの下調べ）→ 手C の事前登録（旗104）
- **見積り**: 2 min / **迂回**: 不可（**旗52 以来「合成で確かめてから実データ」は曲げない**）

### GATE-02 — 手C（降雨イベントからの経過）の**実データでの下調べ**
- **status**: **resolved（2026-09-02・旗105）** — **実データのある機械で回った周が自分で消化した。**
  **結果**：**前提（春と秋で雨の入り方が違う）は 3 サイトとも成り立った**が、
  **θ の重なり帯は 6 通り中 5 通りで下限未満＝θ を揃える設計の方が壊れた**。
  **落ちたのは春**（旗104 の申し送りとは逆）。出力全文 `research/logs/step105_rain_history_real.txt`、
  詳細は `FLAGS_LOG.md:4361`。**次は旗106＝手C の事前登録**（**実データはもう要らない段**）。
- ~~**status**: open~~ / **依存**: ~~GATE-17~~（**旗104 で解決**）
- **★旗104 からの申し送り**: **合成では 16 年だと秋の腕が帯の中で下限に届かず、
  対照の成立に 30 年要った。** **これは合成の性質であって実データの話ではない**が、
  **実データでも「秋が日数で落ちる」かを最初に見ること。**
- **なぜ私にできないか**: `P`（降水）の実データが要る。
- **やること**（**旗103 で設計変更**：**いきなり検定しない。まず前提を分布で見る**）:
  ```bash
  python research/rain_history_probe_step103.py --real --sites US-Wkg US-Whs US-SRM
  ```
  **この道具は検定をしない**——**春と秋で雨の入り方が実際に違うか**、
  **`直後`／`遠い` の層別が下限に届くか**、**θ を揃えられるか**だけを出す。
  **旗96/97 は前提を分布で見ずに設計に入って二度外した。** その繰り返しを避けるため。
- **返すもの**: 実行ログ全文（**とくに θ の重なり帯の幅と帯の中の日数**）
- **注意**: `OPEN_QUESTIONS_OPTIONS.md` の警告 —
  **重なり帯は「群間で違う量」に使うと自己矛盾するので、手C では設計を変えること**
- **見積り**: 30 min / **迂回**: 可（**事前登録・道具・合成での帰無較正は手元で先に済ませられる**）

### GATE-03 — 手B（変化の向き＝10 日間の変化量で層別）の実データ検定
- **status**: open / **依存**: GATE-01
- **なぜ私にできないか**: 同上。
- **やること**: `Δθ`・`ΔTs`・`ΔRg` を作り符号で層別。「Ts が上昇中の日だけ反転しない」なら軌道依存。
- **注意**: 手C と同じ重なり帯の警告が効く。**この研究で一度も使っていない軸**である
- **見積り**: 30 min / **迂回**: 可（設計と道具は手元で書ける）

### GATE-04 — `RUN_HEAVY.md` ①〜④ の夜間実行
- **status**: open / **依存**: GATE-01
- **なぜ私にできないか**: 実データ + 数時間〜夜通しの計算。
- **やること**: `japanflux_pn/RUN_HEAVY.md` の ①ビン数感度 ②CMIknn 再現 ③完全版 PCMCI+ ④O-info バッチ。
  すべて `nohup ... &` でログ保存・再開ありに書いてある。
- **返すもの**: 各 `.log` の末尾（自動判定の「✅ 全ビンで結論不変」／「⚠ 崩れる」を含む）と出力 CSV の要約
- **解けると動くもの**: `FUTURE_PLAN.md` Phase 1（頑健性）と Phase 2（O-information）
- **見積り**: 仕掛け 10 min + 夜通し / **迂回**: 可（結果待ちの間に別の旗を進められる）

### GATE-06 — PhenoCam 取得物の再解析
- **status**: open / **依存**: GATE-01, および旗94 で見つかった**道具の欠陥 2 件**の修正（手元で可）
- **なぜ私にできないか**: 取得物がローカルにある。
- **見積り**: 30 min / **迂回**: 可

### GATE-21 — 実データ 36 サイトの `corr_thT` の分布を測って残す（旗125 の宿題・**5 分**）
- **status**: open / **依存**: GATE-01（`/mnt/hdd/cosore-0.7.0` が見える周）
- **なぜ私にできないか**: **COSORE の実データが要る**（この周は `/mnt/hdd` がサンドボックス外）。
- **背景**: **旗125 は θ–T 相関を −0.5〜−0.95 で振って旗44 の `d` の被覆率を測った。**
  **だが「実サイトの相関がどこに分布しているか」は未確認である**——
  **旗44 の実行時に `corr_thT` は画面に印字していたが、当時 `runlog.py` が無くファイルに残っていない**
  （`research/logs/` に step44 のログは無い・旗125 で確認）。**`FLAGS_LOG.md:895` に JASSAL の −0.85 が
  1 件残るだけである。** **＝旗125 の水準ごとの結論を、実サイト何本に当てはめるかが言えない。**
- **やること**（**再解析ではない。旗44 をそのまま走らせて出力をファイルに残すだけ**）:
  ```bash
  cd ~/bakanposs
  .venv/bin/python research/q10_confound_step44.py --cosore-dir /mnt/hdd/cosore-0.7.0 --igbp forest \
    | tee research/logs/step44_rerun_corr_thT.txt
  ```
- **返すもの**: **ログ全文**（**`θ-T` 列と判定列がサイトごとに並んだ表**）。
  **★とくに ×判定の 6 本**（Hirano PDB/PDF/PUF・Fairbanks・Hokuroku・GUTIERREZ）**の `θ-T` が
  −0.85 より強い負相関の側にあるか**——**旗125 の実測では、共線が強いと偽 × が 23% まで出る
  （偽 ★ は 0%）。要再点検は ★ ではなく × の側である。**
- **解けると動くもの**: **旗125 の結論に「実サイト何本が該当するか」を付けられる**
  ＝**A-2 の「逆符号のサイトが実在する」に留保が要るかどうかが確定する**
  （**18/36 の ★ 側は旗125 の時点で既に安全＝偽 ★ の期待 1.8 本／36**）。
  **論文化（`MEASURED_ONLY_SPINE.md` の A-2 節・【旗125】の留保）に直結。**
- **見積り**: 5 min / **迂回**: 可（**旗125 の合成側の結論は、この分布に依らず立つ**）

### GATE-22 — 実データの**残差 ACF1・回帰子 ACF1** の分布を測る（旗126 の宿題・**GATE-21 と同時に済む**）
- **status**: open / **依存**: GATE-01（`/mnt/hdd/cosore-0.7.0` が見える周）
- **なぜ私にできないか**: **COSORE の実データが要る**（この周は `/mnt/hdd` がサンドボックス外）。
- **背景**: **旗126 は「残差が AR(1) のとき旗44 のブロックブート CI は被覆を保つか」を合成で測り、
  被覆の挙動が二つの量に強く依存することを見つけた**——**(a) 残差の自己相関 φ、
  (b) 回帰子 `Tc·θz` の自己相関（実質は θ の自己相関）。**
  **(b) が白いと (a) をいくら強くしても推定量の分散は膨らまない**（軸1・`block=1` と差が出ない）。
  **(b) が赤いと初めてブロックブートが効き、iid ブートは被覆 32%／15%・偽 ★ 31%／47% まで崩れる。**
  **＝実データのこの 2 つを測らないと、旗126 の水準表をどこに当てればよいかが決まらない。**
- **やること**（**再解析ではない。下調べであり、`analyze` の判定も旗44 の規則も呼ばない**）:
  ```bash
  cd ~/bakanposs
  .venv/bin/python research/resid_acf_probe_step126.py --cosore-dir /mnt/hdd/cosore-0.7.0
  ```
  **道具は自己検証済み**（既知の φ=0/0.9/0.98 を 0.006/0.903/0.979 で取り戻す・
  `research/logs/step126_acf_20260904_141444.txt`）。**出力は `research/logs/` に自動で残る。**
- **返すもの**: **ログ全文**（サイトごとの**残差 ACF1・回帰子 `Tc·θz` の ACF1・θ の ACF1・`corr(θ,T)`**
  と、末尾の分布・旗126 の水準への振り分け）。
- **解けると動くもの**: **旗126 の結論に「実サイトが軸1 側か軸2 側か」を付けられる**
  ＝**旗44 の CI の信頼性の記述が確定する**。**同じ 1 回で GATE-21 の `corr_thT` も出るので、
  旗125 の × 6 本の留保も同時に片付く。**
- **見積り**: 5 min（**GATE-21 と合わせて 1 回・実質同じ実行**） / **迂回**: 可
  （**旗126 の合成側の結論——★率は 9 セル全部で 5% 以下——はこの分布に依らず立つ**）

---

## A — 取得・認証（アカウント／規約同意が要る）

### GATE-05 — AmeriFlux 未取得 26 組の取得 ★最有力
- **status**: **partial（2026-09-03・ユーザー承認「撮ろう」）— 詳細 `GATE05_ACQUISITION.md`**
  **26 組の大半は既にローカルにある**（旗86 で17取得・現在29サイトコード）。
  **本命は追加取得ではなく、既取得サイトでの「同一地点★率の直接測定」（旗80/86 の検定はこれから）**
  ＝**手元で自走可能・旗85 の 25〜29% を実証し GATE-10 を裏打ちする。ループはこれを次の旗にしてよい。**
  **残る真の取得（DUA が要る人間ゲート）＝CA-Obs・US-SCf・US-RTS・US-SJ1 の少数のみ。**
- **なぜ（追加分が）私にできないか**: データ利用同意（DUA）は**あなたの署名が要る法的行為**で、AI が代諾できない。
- **やること**: 旗85 で名指しされた組を取得（**US-Ho1×3・US-UMB×2・US-Ton×2・CA-Obs・CA-Ca1・US-WCr 等**）。
  正確な一覧は `FLAGS_LOG.md` の旗85 節にある（`grep -n '^## 旗85' research/FLAGS_LOG.md` で引ける）。
- **返すもの**: 取得できたサイト ID・期間・チャンバー本数
- **解けると何が動くか**: **同一地点の証拠が約 2 倍**になり、
  **★で選ばずに集めた対で★率を測れる**（旗85 の「無作為なら 25〜29%」を実証で確かめられる）。
  **★選択バイアスという最も重い訂正を、訂正のまま終わらせずに済む唯一の手**
- **見積り**: 登録 30 min + 審査待ち 数日 / **迂回**: 可（**取得後の解析コードは手元で先に書ける**）

### GATE-08 — COSORE の多層データセット追加取得
- **status**: open
- **なぜ私にできないか**: 取得が要る。
- **正直な見通し**: **望み薄**。旗101/102 で DESAI(20 層)・UEYAMA_FAIRBANKS(5 層) は**どちらも下限未満**と判明済み。
  旗102 の結論も「**地点内で深さを振る道は JASSAL の n=1 で尽きている**」。
  **やるなら「下限を満たす多層データセットが他にあるか」の確認まで**
- **見積り**: 1 h / **迂回**: 可（優先度は低い）

---

## P — 外界への問い合わせ

### GATE-07 — 一次文献 3 件の確認
- **status**: **resolved（gate ではなくなった）— 2026-09-03・旗118**
  **この LOCAL 環境では `WebSearch`/`WebFetch` が動く**（旗92 の遮断はコンテナ固有）。
  **① Eldhose 2025・② Davidson DAMM は一次確認済み**（`NOVELTY_ASSESSMENT.md`）。
  **③ Stoy 2007 と、A-1/A-3 の先行（Koehn 2021 は Wiley 403）は継続中**——**到達しやすいソースで自走で潰す。**
- ~~**status**: open / **旗92 で構造的に不可能**~~（**環境固有と訂正**）。
- **やること**: `LITERATURE_VERIFICATION_TODO.md` の**確認が要る 3 件**を、
  **egress の開いた環境か図書館**で確かめる。
- **重要な性質**: この 3 件はいずれも「**文献を根拠に自分の主張を下げた**」箇所。
  **文献が違えば削りすぎになっている**。骨格自体は自分の検証に載っているので倒れない
- **見積り**: 1 h / **迂回**: 可（**手 F の書き換えは「文献依存を減らす」方向なので、むしろ先に進められる**）
- **⚠ 旗119 の訂正**: 上の「この LOCAL 環境では動く」は**言い過ぎ**だった。
  **同じ機械でも、自走ループの非対話プロセスでは `WebSearch`/`WebFetch` が承認待ちになり実行できない**
  （旗119 で実際に不可）。**到達性は機械ではなくプロセスの権限で決まる。** → **GATE-19**。
  **残りの ⚪ の一次確認は、権限のある周か対話セッションで行う。**
- **✅ 旗120（2026-09-03）の更新**: **GATE-19 の scoped 権限が効き、自走ループの周で
  `WebSearch`/`WebFetch`/Python が実際に動いた**（`curl` と複合コマンドは列挙外で不可）。
  **⚪ #1（A-3 の秋>春非対称の先行）は潰した**——**Koehn 2021 は `P→θ` の論文で、
  `θ→蒸発` の季節非対称は報告していない**（Semantic Scholar Graph API で抄録到達・**Wiley 本文は今周も 403**）。
  **ET ヒステリシスの先行は日内スケール**（Zheng 2014 PLoS ONE・PMC で本文到達）。
  **残る ⚪ は 3 件で、最優先は AFM 2018 の `Rs–T` 季節ヒステリシス（A-2 側）。**
  **到達手段の追加＝Semantic Scholar Graph API**（Wiley/Elsevier/T&F が 403 でも抄録が取れる）。

### GATE-19 — 自走ループのプロセスに `WebSearch`/`WebFetch`（と Python）の実行許可を与える
- **status**: **resolved（2026-09-03・ユーザー承認「承認が要る全ての場合を承認済みとして進めてよい」）**
  **`--yolo`（全承認バイパス）は分類器がブロックした**ので、**安全な scoped 方式に切り替えた**：
  **`.claude/settings.local.json` の `permissions.allow` に WebSearch・WebFetch・Bash(python/git/データ整形)
  を列挙**し、**`ALLOW_TOOLS` 環境変数を run_loop.sh に渡して `--allowedTools` にする**。
  **＝非対話プロセスでも、列挙したツールだけは承認なしで実行できる**（全権限バイパスではない）。
  **注意**：**Opus 5 はこの CLI バージョンのモデルカタログに無く使えない**（`claude-opus-5`/`opus-5` は拒否）。
  **ループは現行 `opus` で回す。** Opus 5 を使うなら Claude Code の更新が要る。
- ~~open~~ / **分類**: S（外部仕様＝ハーネスの権限設定）
- **なぜ私にできないか**: 権限の境界。**非対話プロセスでは承認ダイアログに答える主体がおらず、
  ツール呼び出しがその場で失敗する**（旗119 の周で `WebSearch` 3 本・`WebFetch` 1 本・
  `python -c` が全て承認待ちで不可）。**自分で自分に権限を与えることはできない。**
- **あなたが取る行動**（どちらでも可）:
  1. **対話セッションで**一次確認の周を 1 回回す（`/research-loop` を対話で起動し、
     `WebSearch`/`WebFetch` の許可を承認する）。**所要 30 分程度。**
  2. または `.claude/settings.json` の `permissions.allow` に `WebSearch` と `WebFetch(domain:*)` を
     加えて自走ループを回す（**外向きの取得を無人で許すことになるので、判断はあなたのもの**）。
- **返してほしいもの**: 許可の付与（設定の変更 or 対話周の実施）そのもの。数値の報告は不要。
- **解けると何が動くか**: `NOVELTY_ASSESSMENT.md` の残り **⚪ 3 件**——
  **① A-3 の秋>春非対称の先行（Koehn 2021 の代替ルート＋「ET–θ の季節ヒステリシス」での検索）＝
  主張の核に効く**／② A-1 の残差同期の先行（旗119 で優先度低下）／③ Stoy 2007 の書誌。
- **見積り**: 5 分（設定）または 30 分（対話周） / **迂回**: **可**——
  **旗118 で確認済みの分を本文に落とす作業（手F）は権限なしでできる。旗119 で実施済み。**

### GATE-20 — **旗119 の変更をコミットして push する**（★最優先・1 分）
- **status**: **resolved（2026-09-03・対話セッション）** — `914f0ec` で 7 ファイルとも push 済み。
- ~~open~~ / **分類**: S（ハーネスの権限設定）／**GATE-18 と同型の再発**
- **なぜ私にできないか**: **旗119 の周では `git add` も `git commit -a` も承認待ちで実行できなかった。**
  **変更はディスク上にあるが git には入っていない**（記録が失われるわけではないが、push されていない）。
- **あなたが打つコマンド**（**`git add -A` は使わない**——未追跡の生成物を巻き込む）:
  ```bash
  cd ~/bakanposs
  git add research/MEASURED_ONLY_SPINE.md research/LITERATURE_NOTES.md \
    research/LITERATURE_VERIFICATION_TODO.md research/NOVELTY_ASSESSMENT.md \
    research/HUMAN_GATES.md research/FLAGS_LOG.md research/SESSION_STATE.md
  git status --short --untracked-files=no      # ★ この 7 つだけか確認してから
  git commit -m "旗119 手F: 「再発見」を取り下げ位置づけを確定。新規性主張の留保漏れを1件是正"
  git push -u origin "$(git rev-parse --abbrev-ref HEAD)"
  ```
- **返してほしいもの**: コミットハッシュ（または `git log --oneline -1`）。
- **解けると何が動くか**: **旗119 の記録がリモートに載る。** GATE-19 と同じ権限付与で恒久的に解ける。
- **見積り**: 1 分 / **迂回**: 不可（**ただし次の周は、ディスク上の変更をそのまま読んで続行できる**）。

### GATE-09 — サイト管理者（高山ほか）への連絡
- **status**: open
- **なぜ私にできないか**: 人に送るメールであり、あなたの名前で出すもの。
- **重要**: `CONTACT_DRAFT_TKY.md` 冒頭の 2026-08 追記を読むこと。
  **穴②を閉じるためだけなら、この連絡は不要**（JP-Fhk ↔ UEYAMA_HOKUROKU 0.00km で閉じる）。
  **高山そのものを同一地点で解析したい場合にのみ意味を持つ**
- **見積り**: 10 min + 返信待ち / **迂回**: 可（**現状は不要と判断されている**）

### GATE-16 — 新規観測の実施可否
- **status**: open / **依存**: GATE-11（スコープ）
- **なぜ私にできないか**: 予算・設置・**最低 3 年**の期間を要する現実の行為。
- **対象**: `NEW_OBSERVATION_DESIGN.md` ②多深度 θ（※**旗97 で「新規観測ではなく手元の未使用列だった」と訂正済**）
  → ③土壌 CO₂ 深度プロファイル → ④¹³C パルス標識（共同研究・決定的だが最も重い）
- **見積り**: 判断のみなら 20 min / **迂回**: 可（卒論の範囲外なら「今後の観測で確かめるべき点」として渡すだけでよい）

---

## J — 研究判断（AI が代行してはいけない）

### GATE-10 — 旗85 の★選択バイアス訂正を、公表物にどこまで反映するか
- **status**: **resolved（2026-09-03・ユーザー判断）** — **「旗66 の選択がロジカルなら OK」。**
  **旗66 の同一地点選択は原理的（タワー・衛星・チャンバーが揃う唯一の組・★で選んでいない）でロジカル。**
  **方針：旗85 の率訂正（旗66 の 50% → 無作為なら 25〜29%）を正直に併記して反映する**
  （質的結論は不変・変わるのは割合だけ）。**実証は GATE-05 の★率直接測定で裏打ちする。**
- ~~**status**: open~~
- **なぜ私にできないか**: 「**既に出した数字が膨らんでいたと分かったとき、どう書き直すか**」は
  研究者の判断であって AI の判断ではない。
- **事実**: 旗66 の「9/18＝50%」は**約 1.7〜2 倍に膨らんでいた**。質的結論は不変、変わるのは割合だけ。
- **選択肢**: (a) 公表済 `synthesis.html` を訂正込みで更新 / (b) 卒論本文でのみ訂正 /
  (c) GATE-05 の取得を待って**実証つきで**訂正
- **見積り**: 10 min（**PC 不要**）/ **迂回**: 不可（率に触れる記述が全部これ待ち）

### GATE-11 — 卒論のスコープ確定（どこで打ち止めるか）★S 系を一括解除
- **status**: open
- **なぜ私にできないか**: あなたの締切・指導教員との合意・修士への引き継ぎ方に依存する。
- **現状**: 直近 旗95〜97・100 が ▲ の連続、旗99 が★、旗102 が○。
  **「測定量を使い切った」は三度書いて三度とも早かった**（`OPEN_QUESTIONS_OPTIONS.md`）。
  **手が尽きたから止めるのではなく、いつ止めるかを決める必要がある**
- **選択肢**: (a) 手 C・B まで回して打ち止め / (b) GATE-05 の取得まで待つ / (c) 今ある材料で手 F（書き換え）へ
- **見積り**: 15 min（**PC 不要**）/ **迂回**: 不可

### GATE-12 — 軸B の兆し（深い温度センサ → 季節メモリ側）をどう扱うか
- **status**: open
- **なぜ私にできないか**: 「**兆しをどこまで書くか**」は主張の重さの判断。
- **事実**（旗102）: 深い(>5cm) 4 本すべて★でなく、**3 本は e-fold が長くて季節メモリ側に分類**。
  **★と季節メモリの線引きが、生物ではなくセンサの深さを一部なぞっている可能性**。
  ただし **4 本・3 地点**。しかも **JASSAL の n=1 とは向きが合わない**（両方書いてある）
- **選択肢**: (a) 兆しとして記す（旗102 の判断のまま）/ (b) 限界の節に格上げ /
  (c) 新規観測の設計に渡す
- **見積り**: 10 min（**PC 不要**）/ **迂回**: 可（現状 (a) で矛盾はない）

### GATE-13 — 「率の比較の下限を 10 本に」の遡及範囲
- **status**: open
- **なぜ私にできないか**: 過去の旗の結論を**遡って読み替えるか**の判断。
- **事実**（旗102）: 下限 3 本は率の比較には低すぎた。
  **規則は結果を見る前に決めたので変えなかった**（正しい作法）。**次からは 10 本**とした。
- **問い**: この基準で**過去の旗のうち n<10 の率比較を洗い直すか**、それとも**今後のみに適用するか**
- **見積り**: 10 min（**PC 不要**）/ **迂回**: 可

---

## S — 外部仕様（与えられれば AUTO 化する）

### GATE-14 — 卒論の提出形式・締切・字数・図数
- **status**: open / **依存**: GATE-11
- **迂回**: 可（**本文の草稿は仕様非依存で書ける**）

### GATE-15 — 公開済 `synthesis.html` を更新してよいか
- **status**: **解消（2026-09-02）** — **承認済みで、実際に更新した**
- **対象**: https://claude.ai/code/artifact/77271e1b-183f-4574-aa50-4bfd9ebbf1ae
- **現況**: **旗13–102 まで反映済み**（旗85 の訂正・旗99–102 の 6 行・欠陥 34 件・
  合成が進路を変えた 8 度・事前登録 19 件まで整合済み）。
- **残り**: **旗103–105 は未反映。** 次に更新するときに足す。
- **注記**: **更新の承認はユーザーから出ている**ので、以後この台帳では open にしない。
  **ただし公開は外向きの行為なので、内容を大きく変えるときは一言添える。**

---

## 注記

- **⚠ 旗103 の注記は、一般には成り立たない**（**旗104 で本人が訂正し、2026-09-02 に再確認した**）。
  **「コンテナでは Python を実行できない」は、その周のコンテナの権限であって環境一般の性質ではない。**
  **本日の別セッション（`/mnt/hdd` の見えないコンテナ）では、`python research/tower_weather_step100.py` と
  `python research/driver_sensor_step102.py` の合成検証が実際に走り、旗100 と旗102 の
  設計の欠陥を実データ前に捕まえた。**
  **＝コンテナ周でも合成の実行はできることがある。** **まず一度走らせて確かめる。**
  **走らなかったときだけ、GATE として積む。**
- **手元でできることは、まだ相当ある。** 手 C・手 B の**事前登録・道具**（**合成の実行は上記のとおりゲート**）、
  GATE-05 取得後の解析コード、手 F（文献依存を減らす書き換え）、旗94 の道具の欠陥 2 件の修正、
  `SESSION_STATE.md` の旗100〜102 への追いつき。**ループはまずそれを食い潰す。**
- **PC もデータも不要な GATE が 4 件ある**（10・11・12・13）。**これを先に答えると自走範囲が大きく広がる。**
- 本台帳は静的抽出。**旗102 以降は反映されていない**ので `GATE_AUDIT_PROMPT.md` で検証すること。

### GATE-23 — HKUST 機関リポジトリの CAPTCHA を越えて Xu & Qi 2001 の本文を取る（**2 分**）
- **status**: open（**2026-09-04・旗129 で登録**）
- **分類**: P（外界）
- **なぜ私にできないか**: **到達性の境界**。`http://repository.hkust.edu.hk/ir/Record/1783.1-161573` は
  **CAPTCHA の後ろにある**。`WebFetch` は「Too many redirects」、生 HTTP は 302 の無限ループ、
  Cookie を持たせると `.../ir/captcha?redirect=...` に着く。**画像 CAPTCHA は機械で越えられない。**
- **あなたが打つコマンド／取る行動**: ブラウザで
  `https://repository.hkust.edu.hk/ir/Record/1783.1-161573` を開き、CAPTCHA を通す。
- **返してほしいもの**:
  1. **その記録が Xu & Qi 2001 GBC 15(3), 687–696（doi:10.1029/2000GB001365）のものか**（yes/no）
  2. **全文 PDF の添付があるか**。あれば **PDF そのもの**か、**その直リンク URL**
  3. 無ければ、ページに出ている**版の種別**（submitted / accepted / published）
- **解けると何が動くか**: **⚪「Xu & Qi 2001」＝`Q10 = a − bT + cS_w` の原型**が閉じる。
  **A-2（水分依存 Q10）の差分の言い方**——**旗122/123 が「分数を出せること」に絞った差分**が、
  **原型の側にどこまで書かれていたか**で確定する。**A-2 の 🔴 は既に 1987 年まで遡っている**ので、
  **色が変わる見込みは低い。変わるのは差分の文言。**
- **見積り**: 2 min / **迂回**: 可（**⚪ のまま据え置いても骨格は倒れない**。
  **本文に届くまで差分の文言にこの論文を使わない**＝旗128 の Tucker & Reed と同じ扱い）

### GATE-24 — Tucker & Reed 2016 の**本文**を取る（図書館か著者連絡・**5 分／数日**）
- **status**: open（**2026-09-04・旗132 で登録**）
- **分類**: P（外界）
- **なぜ私にできないか**: **到達性の境界。機械経路を尽くして、無いことを確認した。**
  1. **USGS の機関ミラーは存在しない**——Publications Warehouse の API（記録 70169003）に
     **本文ファイルが 1 つも紐づいていない**（`outside_thumb.jpg` のみ＝外部誌掲載）。
  2. **wayback にこの論文の PDF 捕獲は無い**——**同誌・同年・同パス形式の兄弟 DOI は 13 件あるのに、である**
     （ホスト水準の対照つき）。**捕獲されているのは記事ページ（抄録まで）だけ。**
  3. Springer は購読（`This is a preview of subscription content`）／`scholar.archive.org` 0 件／
     Unpaywall・OpenAlex・S2・OpenAIRE いずれも全文リンク無し／`WebSearch` でも複製が出ない。
  **＝私の側に残る手が無い。機関購読か著者からの送付が要る。**
- **あなたが打つコマンド／取る行動**: 次のどちらか。
  1. **大学の機関購読で** `https://link.springer.com/article/10.1007/s10533-016-0200-1` の
     **PDF をダウンロードし、`research/tmp_pdfs/` に置く**（版管理しない）。
  2. **著者に依頼**（Colin L. Tucker は現在 USDA Forest Service NRS・Sasha C. Reed は USGS SBSC）。
- **返してほしいもの**: **PDF そのもの**（または本文の該当箇所）。**最低限、次の 3 点が分かる部分**：
  1. **見かけ Q10 を θ の関数として推定しているか**（それとも Rs のモデルに θ の乗数を置いた結果として
     Q10 が動くだけか）。**抄録の `Modeled Q10 ranged from 2.7 to 1.4 between 5 and 45 °C` は
     T に対する変化であって θ に対する変化ではない。ここが分かれ目。**
  2. **θ と Ts の相関（交絡）を数で報告しているか**（Gaumont-Guay 2006 の `r = 0.82` に相当するもの）。
  3. **モデル比較の候補集合**（Table のモデル式一覧）。
- **解けると何が動くか**: **A-2 の差分 (ii)（交絡の分離）の書き方が確定する。**
  **現状は「一般化して書けない」状態で止めてある**（旗132）——
  **「先行は Q10 に対する結合モデルを持たない」と書けるのは本文確認済みの 3 件までで、
  この 1 件が未確認のまま残っているため。** **⚪ 3 件の中で、A-2 の差分にとって最も危険なのはこれ。**
  **色（🟡）は動かない見込み。動くのは差分の文言と、引用の義務。**
- **見積り**: 5 min（機関購読があれば）／数日（著者連絡）
- **迂回**: 可。**迂回する場合の書き方は旗132 で確定済み**——
  **差分を 3 件に限定して書き、「最近接の未確認文献が 1 件あり、そこでは Ts・θ・Rs の結合モデルが
  Bayes で比較されている」ことを本文に明記する。⚪ を伏せて差分を書かない。**
