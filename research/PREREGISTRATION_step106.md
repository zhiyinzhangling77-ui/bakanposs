# 事前登録：旗106「**Bowen 反転は、雨からの日数で説明できるか**」（手C）

**実データの相関はまだ一度も計算していない。**
**見たのは旗105 の下調べ（日数・年数・θ の中央値・二軸の相関）だけである。**（20回目。）

---

## **前提の事実確認**（旗96/97/102 の反省・**旗105 の実出力から**）

### 事実①：**前提は成り立つ**——**春と秋で「雨からの日数」の構成が実際に違う**

旗105 の実出力（`θ高×Rg高` セル・`直後`＝最後のイベントから ≤3 日／`遠い`＝≥7 日）：

| サイト | 春 直後 / 遠い | **春の直後の割合** | 秋 直後 / 遠い | **秋の直後の割合** |
|---|---|---|---|---|
| US-Wkg | 47 / 502 | **8.6%** | 75 / 173 | **30%** |
| US-Whs | 44 / 353 | **11%** | 68 / 140 | **33%** |
| US-SRM | 84 / 343 | **20%** | 96 / 110 | **47%** |

**＝秋は春の 2.4〜3.5 倍、「雨の直後」の日を持つ。**
**旗96・97 と違い、前提そのものは崩れていない。**

### 事実②：**春は主検定にできない**
**春の `直後` は US-Wkg 47 日・US-Whs 44 日で下限（60 日）未満**（旗105）。
**春で層別できるのは US-SRM だけ＝独立クラスタが 1 つ**になる。
**春を主検定にすると n=1 になるので、しない。**

### 事実③：**二つの層別軸は、ほぼ同じ量である**
**`dryspell`（最後のイベントからの日数）と `直前 7 日積算 P` の Spearman r ＝ −0.79 / −0.74 / −0.72**（旗105）。
**両方を独立な軸として並べるのは、同じ量を二度使うことに近い。**

### 事実④：**重なり帯は使えない**
**θ を揃える形は 6 通り中 5 通りで下限未満、通った 1 つも縁**（旗105）。**この形は捨てる。**

---

## **旗105 の申し送りのうち、そのままでは実装できないもの**（**先に書く**）

旗105 は「**θ を共変量に入れる形で登録する**」と申し送った。
**これはそのままには実装できない。** **θ は本検定の曝露そのものだからである**
（測っているのは **θ→γLE・θ→γH の偏相関**）。**θ を統制すれば、測る対象が消える。**

**代わりに、こうする**（**申し送りの意図——「θ の違いを無視しない」——は保つ**）：
1. **各層の θ の分布（中央値・四分位）を必ず併記する。**
2. **層間で θ の範囲が重なる部分に限った感度解析**を、**日数が下限を満たすときだけ**添える。
   **満たさなければ「できなかった」と書く**（**旗105 が帯で落ちたのと同じ理由なら、そう書く**）。
3. **交絡は解けないと明記する**——**`直後` の日は `遠い` の日より湿っている**（旗105：
   春 θ 中央 12.32 対 9.78・7.39 対 4.52・6.29 対 5.03）。
   **層別の差が「雨からの日数」によるのか「θ の水準」によるのかは、この設計では分けられない。**

---

## 問い

**春が反転しないのは、春の日が「雨から遠い」日ばかりだからか。**

**論理**（**事実①がこれを可能にしている**）：
- **もし反転に「最近濡れたこと」が要るなら**、
  **秋の `遠い` 層でも反転は消えるはず**である。
- **秋の `遠い` 層でも反転が残るなら**、**`遠い` であること自体は反転を妨げない**
  ＝**春が反転しない理由は「雨からの日数」ではない。**

**＝秋の中だけで決着する。** **春と秋を比べる必要が無い**（比べられないので、これは設計上の利点である）。

## 手順（先に固定する）

1. **対象は秋の 3 サイト**（US-Wkg・US-Whs・US-SRM）。**季節は旗89 以来の `AUTUMN=(9,10,11)`。**
2. **セルは旗89 の `cell_of`（θ・Rg の中央値で 4 分割）の `θ高×Rg高`**——**旗96/105 と同一。**
3. **層別軸は `dryspell`（主）**：**`直後` ≤3 日／`遠い` ≥7 日**（旗103–105 と同一のしきい値）。
   **`直前 7 日積算 P` は感度解析に回す**（事実③）。
4. **統計量は旗36 以来と同一**：**Rg を統制した θ→γLE・θ→γH の偏 Spearman**、
   **年ブロック・ブートストラップの CI**。
5. **「反転」の定義も同一**：**θ→γLE > 0 かつ θ→γH < 0 で、どちらの CI も 0 を跨がない。**
6. **下限**：**各層・各サイトで 60 日・3 暦年**（旗105 で秋の 6 層すべてが満たすことを確認済み）。
7. **必ず併記する**：**各層の日数・年数・θ の中央値と四分位・両偏相関と CI。**

## **門①（対照）——旗104 の失敗を踏まえて先に決める**

**旗104 は「陽性の対照が、自分の名乗る性質を持っていなかった」で止まった。**
**同じ形を避けるため、門を二つ置く。**

**門①-a（実データ側）**：**そのサイトの秋全体（層別せず・同じセル・同じ統計量）で反転すること。**
**反転しなければ、そのサイトは判定しない。**
**理由**：**旗96 の秋の値（+0.51/+0.50/+0.65）は「重なり帯の中」で測ったものであり、
本検定は帯を使わない**——**指定が変わった以上、対照は測り直す必要がある。**
**この門は層別の結果とは独立に決まる。**

**門①-b（合成側）**：**合成で「期待する枝に実際に到達すること」を確かめてから実データに触れる。**
**「期待どおりに出るはず」と書いただけの対照は対照ではない**（旗104 が形として記録した規則）。

## 判定規則（**先に決める**）

| 結果（門①-a を通ったサイトの中で） | 結論 |
|---|---|
| **`直後` で反転し、`遠い` で反転しない**サイトが **3 中 2 以上** | **★手C が説明する**——**「季節」を「雨からの日数」で言い直せた** |
| **両層とも反転する**サイトが **3 中 2 以上** | **▲手C は説明しない**——**`遠い` でも反転する以上、春の不反転の理由ではない** |
| **`遠い` で反転し `直後` で反転しない**サイトが 1 つでもある | **○予想と逆**。**そのまま書き、まとめない** |
| 上のどれにも当てはまらない | **○中間**。**サイトごとに書く** |
| **門①-a を通るサイトが 2 未満** | **判定しない**（**どのサイトがなぜ落ちたかを書く**） |

**閾値「3 中 2」は恣意的だが、いまここで固定する。**

### 参考（**主判定には使わない**）
- **春の US-SRM**（`直後` 84 日・19 年／`遠い` 343 日・22 年）で同じ層別をする。
  **手C が正しければ、春でも `直後` では反転するはず**である。
  **n=1 なので参考**。**「確かめた」とは書かない。**
- **`直前 7 日積算 P` による層別**（感度解析）。**主判定と食い違えば、両方書く。**

## 予測（**低い確信で書く**）

**H1**：**▲手C は説明しない**（**秋の `遠い` 層でも反転が残る**）。
**理由**：**θ・Rg・Ts・GCC・VPD・深層θ の六つがすべて外れてきた**という帰納である。
**機構の理解ではない。**

**確信は低い。事前予測は 9 回中 4 勝**（旗91・99・100・102 当たり）。
**旗102 で「物理からの予測」を書いて向きを間違えたばかりなので、
今回は合成で向きを確かめるまで、この予測を上げない**（**下の §合成**）。

## 合成データで先に検証する（**旗100/102/104 の作法を全部通す**）

**四つ作り、四つとも「期待する枝に実際に到達すること」を確かめる**：

- **`rain_driven`**：**反転が `直後` の日にだけ起きる**世界 → **★を返すべき**
- **`rain_free`**：**反転が dryspell に依らず起きる**世界 → **▲を返すべき**
- **`theta_driven`**：**反転が θ の水準だけで決まり dryspell とは無関係**な世界
  → **▲を返すべき**（**交絡で誤って★を返さないかの試験**——**これが最も重要**）
- **`no_reversal`**：**そもそも反転しない**世界 → **門①-a で落ちるべき**

**規則**（旗100/104 で二度踏んだ形）：
- **否定側の合成も、肯定側と同じ門を通る強さで作る。**
- **各合成が実際にどの枝に到達したかを印字し、到達しない枝があれば合成を較正する**
  （**較正するのは合成の生成側だけ。判定規則・しきい値には触れない**）。
- **`theta_driven` で★が出るなら、この検定は交絡と本物を区別できない＝実データに進まない。**

## 事前に認めておく限界

- **交絡は解けない**（上の §申し送り）。**`直後` の日は湿っている。**
  **言えるのは「`遠い` 層で反転が残るかどうか」までで、
  「雨からの日数そのものの効果」ではない。**
- **春は主検定に入らない。** **結論は「秋の中で `遠い` でも反転するか」に限られる。**
  **春の不反転を直接説明したわけではない**——**「説明の候補として残るか消えるか」だけが決まる。**
- **`dryspell` は `P` から作った量**である。**P は独立測定の 8 つの一つ**だが、
  **イベントのしきい値（5 mm）は我々が決めた**（旗103）。**恣意性は残る。**
- **3 サイトのうち US-Wkg と US-Whs は Walnut Gulch で近接**（旗82 で 1 クラスタ）。
  **独立クラスタは 2（Walnut Gulch・Santa Rita）である。** **「3 サイトで確かめた」とは書かない。**
- **旗105 の観察**（春の θ高 日の大半は `遠い` 側だが、θ 中央は `直後` の方が高い）は
  **後付けの観察であり、本検定の仮説ではない。** **結果の解釈に持ち込まない。**

## 記録

- 本文書は実データ実行**前**に commit。結果はこの文書を書き換えず `FLAGS_LOG.md` に追記する。
- **合成で設計の欠陥が出たら、追補を書いて実データ前に commit する**（旗89・91・96・97・100・102 と同じ）。
- 規則通りに結論を書く。**外れたらスコープを縮める。**

---

## 追補（**実データ実行前**・合成検証で**最重要の対照が対照になっていなかった**）

### 欠陥：**`theta_driven` は交絡の試験になっていなかった**（**旗104 と同じ形・3 度目**）

本文で「**最も重要**」と書いた `theta_driven`（反転が θ の水準だけで決まる世界）は、
**θ を雨の直後性から切り離して作っていた**。**結果、両層の θ 中央が 0.21 と 0.21 で揃った**
——**交絡がまったく入っていない。**

**実データはその形ではない**（旗105：`直後` の θ 中央は `遠い` より高い。
春 12.32 対 9.78・7.39 対 4.52・6.29 対 5.03）。
**＝「名乗る性質を持たない対照」**。**旗100（否定側が★の関門に届かない）・
旗104（陽性の対照が陽性を出せない）に続いて 3 度目である。**

### 修正（**判定規則には触れていない**）
**合成を二つ足した**（**動かしたのは生成側だけ。`MIN_DAYS`/`MIN_YEARS`・
`RECENT_MAX`/`REMOTE_MIN`・`PRIMARY_THR`・`cell_of` は一つも変えていない**）：

- **`theta_partial`**：**θ が雨の直後性に「部分的に」従う**（＝実データの形）。
  **`recent` の係数を掃引して選んだ**——0.015→θ 比 1.02／**0.05→1.07**／0.12→1.18 だが、
  **0.12 では `遠い` が 51 日に潰れる**。**0.05 が「`遠い` が下限を超える最大の交絡」**である。
  **この対照だけ 30 年**にした（22 年では `遠い` が 61 日で縁すぎる。旗104 の `separable` と同じ理由。
  **実データ側の年数はいじっていない**）。
- **`theta_confounded`**：**交絡が極端**な場合（θ = 0.10 + 0.30·recent）。

### 修正後の合成結果（**実データ実行前**・**6 枝すべて到達**）

| 合成 | 直後 | 遠い | 判定 | 期待 |
|---|---|---|---|---|
| `rain_driven` | **反転**（+0.92／−0.93） | 反転せず（−0.10／+0.07） | **★** | ✔ |
| `rain_free` | **反転** | **反転** | **▲** | ✔ |
| `theta_driven` | **反転** | **反転** | **▲** | ✔ |
| **`theta_partial`** | **反転**（184 日・θ 0.23） | **反転**（132 日・θ 0.22） | **▲** | ✔ **偽の★は出ない** |
| `theta_confounded` | **反転**（200 日・θ 0.33） | **27 日・下限未満** | **判定しない** | ✔ |
| `no_reversal` | — | — | **門①-a で落ちる** | ✔ |

### **決定的な点：★の枝は、この範囲では偽陽性を出さない**
**実データくらいの部分交絡（`theta_partial`）で、反転が θ だけで決まる世界を作っても、
道具は ▲ を返した。★ を返さなかった。**
**＝「★が出たら交絡と区別できないので★を読めない」という制限を、置く必要が無かった。**
**判定規則は本文のまま変更しない。**

### 付随して分かった構造（**記録しておく**）
**θ と雨の直後性の交絡が強いほど、`遠い` 層そのものが痩せる**
（θ 比 1.07 で `遠い` 132 日 → 極端な交絡で 27 日）。
**＝この設計は部分的に自己防衛的である**——**交絡が偽の★を作れるほど強いときには、
その前に `遠い` が下限を割り、道具は判定を拒む。**
**旗97 で「重なり帯が仮説の下で自壊する」と書いたのと同じ構造だが、
今回は自壊が誤答ではなく棄権として現れる。**

### 作法（**三度目なので、強く書く**）
> **合成の対照は、「名乗る性質を実際に持っていること」を数値で確かめて初めて対照になる。**
> **`theta_driven` は「交絡の試験」と名乗りながら、両層の θ 中央が同じだった。**
> **対照を書いたら、その対照が名乗る性質を印字して確かめる。**
