# 事前登録 step139 の**追補 D**（門①を走らせた周・旗140）

**本追補は実データの季節 Δ̂ を一度も計算していない状態で書き、commit する**（旗107 の作法）。
**この時点で見たのは門① G1〜G4 の結果だけである**——**`ES-FcO` の θ→γH・θ→γLE の
春秋の値も Δ̂ も、まだ一度も出していない。**

**判定表の 4 行・主判定（θ→γH の Δ = r_SON − r_MAM）・下限（各季節 60 日・3 暦年）・
予測 H1 は一つも変えていない。** 変えたのは **「有意」の決め方だけ**である（追補 D-2）。

---

## 追補 D-1：**門① G4 が落ちた**——**`diff_boot` の CI は、この地点ではそもそも季節を見ていない**

**G4（偽の季節）は事前登録が実データの前に宣言した 4 本のうちの 1 本である**：

> **季節ラベルを日付から切り離して年内で無作為に付け替えた偽データで Δ を出し、CI が 0 を跨ぐこと。
> 跨がなければ、Δ は季節ではなく日数の非対称を拾っている。**

**合否のしきい値（0.90）は、G4 を一度も走らせる前に道具の docstring に固定した**
（`phase_asymmetry_step139.py` の冒頭・旗139a の帰無で断定が 6% 出たので 10% を許容幅にした）。

**結果：200 本の偽の季節のうち、Δ(θ→γH) の CI が 0 を跨いだのは 159 本＝0.795。**
**＝季節の中身を消しても、5 本に 1 本は「季節差がある」と言ってしまう。**

**これは旗139a の欠陥 #66（年ブロックが 3 つだと `diff_boot` の CI が壊れる）が、
合成ではなく実データの上で出たものである。** **合成の被覆 0.46〜0.82 と整合する。**

**帰結（規則どおり）**：**門①が 1 本落ちた以上、事前登録の判定表を `ES-FcO` の実データに当てて
★／▲を読むことはできない**（旗96 で MN-Hst を門①で落としたのと同じ扱い）。
**判定表の「CI が 0 を跨がない」という条件が、この標本では意味を持たないからである。**

## 追補 D-2：**有意の決め方を、置換検定に差し替える**（**判定表の形は変えない**）

**壊れているのは CI であって、統計量（Δ̂ = r_SON − r_MAM）ではない。**
**そこで「有意」の判定だけを、較正できる参照分布に差し替える。**

| | **元の規則（step139 本体）** | **差し替え後（本追補）** |
|---|---|---|
| 主判定の統計量 | **Δ̂ = r_SON − r_MAM（θ→γH）** | **同じ（変えない）** |
| Δ の有意 | `diff_boot` の 95%CI が 0 を跨がない | **季節ラベルの置換 2000 本の両側 p < 0.05** |
| 単季の「反転あり」 | `_boot_ci` の 95%CI が 0 を跨がず r<0 | **θ の年内 7 日ブロック巡回シフト 2000 本の片側 p < 0.05 かつ r<0** |
| 判定表の 4 行 | ★／▲／判定しない／○弱い証拠 | **一字も変えない** |
| 下限 | 各季節 60 日・3 暦年 | **変えない** |
| 予測 H1 | ▲（低い確信） | **変えない**（**通算 16 回中 9 勝の続きとして数える**） |

**置換の作り方（先に固定する）**

1. **Δ の帰無**：**年ごとに MAM+SON の日をプールし、季節の枚数を保ったまま札を振り直す**
   （**G4 と同じ作り方**）。**日数の非対称（春 175・秋 131）はそのまま残り、季節の中身だけが消える。**
   **2000 本。両側 p = (1 + #{|Δ*| ≥ |Δ̂|}) / (2001)。**
2. **単季の帰無**：**その季節の中で、θ の系列を年ごとに 7 日ブロックの巡回シフトで回す**
   （`gH`・`Rg` の行は動かさない）。**巡回シフトは θ の自己相関を保存する**——
   **日次を素朴に並べ替えると自己相関が消えて偽陽性が増える**（旗126 が残した穴と同じ問題）。
   **2000 本。片側 p = (1 + #{r* ≤ r̂}) / (2001)。**
3. **どちらも `diff_boot` を呼ばない**（点推定だけなので速い）。

## 追補 D-3：**使う前に較正する**（**これを通らなければ実データに当てない**）

**置換検定そのものを、旗139a の合成 3 種に当てて較正する。**
**合否のしきい値をここで固定する（実行前）**：

| 合成 | 要求 |
|---|---|
| `none`（反転が無い世界） | **Δ の p < 0.05 が出る割合 ≤ 0.07**（名目 0.05・200 replicate の揺れを見込む） |
| `phase_driven` | **★の枝に到達する割合 ≥ 0.80** |
| `calendar_driven` | **▲の枝に到達する割合 ≥ 0.80** |

**`none` の偽陽性が 0.07 を超えたら、置換検定も使わない**——
**そのときは `ES-FcO` について「この標本では判定できない」で閉じ、量は記述としてのみ残す。**
**判定表を三度目に作り替えることはしない。**

## 追補 D-4：**門① G1 の月が食い違っていた**（**事前登録の見落とし・記録のみ**）

**事前登録の G1 は「同じ実装を US-SRM の秋に当てて旗88 の値（+0.81 / −0.56）を再現すること」と書いた。**
**だが旗88 の「秋」は 9–10 月であり、本検定の SON は 9–11 月である。月が違う。**

**合否は旗88 と同じ 9–10 月で取った**（**そう決めてから走らせた**）：
**θ→γLE = +0.806（旗88 +0.81）・θ→γH = −0.560（旗88 −0.56）＝○合格。**
**参考に印字した SON（9–11 月）は +0.722 / −0.440 で、反転は残るが弱い。**

**＝A-3 側の「秋」と本検定の「SON」は同じ月ではない。**
**この食い違いは判定表を動かさないが、`ES-FcO` の Δ を A-3 の Δ と並べるときには効く。**
**限界として書き足す**（`PREREGISTRATION_step139.md` の「事前に認めておく限界」に一つ足す扱い）。

**G1 のもう半分（旗106 の `遠い` 層：+0.61 / −0.39）は本周では走らせていない。**
**理由：旗106 の値は `θ高×Rg高` セルの中の量であり、step139 は事実③のためセルで切らない設計だから、
同じ実装では出せない。** **＝G1 は旗88 側だけで取った。合格の根拠は旗88 の 2 値である。**

## 変えていないもの（確認）

- **主判定＝θ→γH の Δ = r_SON − r_MAM**（変更なし）
- **判定表 4 行**——**一字も変えていない**
- **下限＝各季節 60 日・3 暦年**（変更なし。**在庫は旗138 の事実④と一致：MAM 175＝63/56/56・SON 131＝40/56/35**）
- **予測 H1＝▲（低い確信）**（変更なし）
- **層についての一文（GATE-26）・追補 C の一文**（変更なし。**必ず付ける**）

---

## 追補 D-5：**較正の結果を、実データの Δ̂ を見る前に確定させる**（**旗141・2026-09-09**）

**本節は `ES-FcO` の θ→γH・θ→γLE・Δ̂ を一度も計算していない状態で書き、commit する**（旗107 の作法）。
**この時点で見たのは合成 3 種の較正の数字だけである。**

### 自己点検（較正より先に走らせた・`--permcheck`）

| 点検 | 結果 | |
|---|---|---|
| **高速経路 `_prep`+`_fast_r` は `partial_spearman` と同じ数を返すか**（60 対） | **最大差 6.661e-16**（許容 1e-10） | **○** |
| **巡回シフトは θ の自己相関を保存するか** | **観測 lag-1 +0.889 → 巡回シフト +0.827／素朴な並べ替え −0.007** | **○** |

**＝追補 D-2 が巡回シフトを選んだ理由が数字で出た。素朴な並べ替えは自己相関を完全に壊す。**

### 追補 D-3 の較正（`--permcal --reps 200 --nperm 2000`・**しきい値は実行前に固定済み**）

| 合成 | 見る量 | 得た値 | 要求 | |
|---|---|---|---|---|
| `none` | **Δ(θ→γH) の p<0.05 が出る割合** | **0.030**（6/200） | ≤ 0.07 | **○** |
| `phase_driven` | **★植生起因と整合 に到達する割合** | **1.000**（200/200） | ≥ 0.80 | **○** |
| `calendar_driven` | **▲植生起因ではない に到達する割合** | **1.000**（200/200） | ≥ 0.80 | **○** |

**＝置換検定を `ES-FcO` の実データに当ててよい。**

**併記すべき数（合否には使わないが、正直に残す）**
- **`none` の θ→γLE（併記側）の偽陽性は 0.070**——**要求 0.07 のちょうど境界である。**
  **要求は主判定の θ→γH に対して書かれているので合否は変わらないが、
  併記側の p は主判定と同じ強さでは読めない。**
- **`none` の判定表の分布は 判定しない 186/200・○弱い証拠 13/200・★植生起因と整合 1/200。**
  **＝反転が無い世界でも 0.5% は ★ が出る**（旗139a の CI 版は 6% だった。**置換版で大きく下がった**）。
- **`none` の真値 Δ は厳密な 0 ではなく −0.047**（60 年の大標本）。**0.030 はそれに対する率である。**

### ★**G4 は追補 D の下では対照として働かない**（**この事実を先に書いておく**）

**G4（偽の季節）の作り方と、追補 D-2 の Δ の帰無の作り方は同一である**
（**年ごとに MAM+SON をプールし、季節の枚数を保ったまま札を振り直す**）。
**＝置換検定で G4 を測れば、偽陽性率は構成上ほぼ厳密に α になる。合格しても証拠にならない。**

**だから「追補 D で G4 が通るようになった」とは書かない。**
**較正の荷を負うのは、(1) 合成 `none`（θ の自己相関と季節構造を持ち、置換の交換可能性の仮定を
実際に試す）と、(2) 旗140 で通った G1・G2・G3 である。**
**G4 は「CI 版を棄却した」という役目を果たして終わった対照である。**

### 次に走らせるもの（**この節を commit した後**）

`python research/phase_asymmetry_step139.py --real`
**判定は `verdict_perm`。CI 版の `verdict` は参考印字のみで判定に使わない。**
**予測 H1（▲・低い確信）は次の周で数える。通算 16 回中 9 勝の続き。**

---

# 追補 E（**旗143・2026-09-09・実行前に固定**）——**旗142 の「見立て」を検定にする**

## なぜ書くか

**旗142 は判定を「○弱い証拠」で閉じた。追補 E はその判定を一切変えない。**
**変えないことを先に宣言しておく**——**追補 E は事後診断であり、結果がどうであれ
旗142 の判定表の行も、予測 H1 の勝敗（外れ）も動かさない。**

**旗142 が残した未検証の見立てはこれである**：

> **r は 0.059 しか違わないのに p は一桁以上違う。しかも標本は MAM（175 日）のほうが多い＝検定力の差ではない。**
> **見立て（未検証）：MAM のほうが θ の自己相関が強く帰無分布が広い。**

**この見立ての正否で、`ES-FcO` の書き方が変わる**——正しければ
「秋の r はその季節の帰無に照らせば珍しい」としか書けない。外れれば別の説明を探すことになる。
**未検証の見立てを未検証のまま次の旗に持ち越さない。**

## 見る量（`--nulldiag`・道具は既存の `shift_season_p` を使い回す）

**季節ごとの巡回シフト帰無分布 {r*}（θ→γH・2000 本）の**
**中央値 m・標準偏差 s・5%/95% 分位**。**観測 r̂ は動かさない。**

## 読み方（**実行前に固定する。数字を見てから足さない**）

| 記号 | 見るもの | 「支持」とする条件 |
|---|---|---|
| **E-1 幅** | `s_MAM / s_SON` | **≥ 1.3** なら「帰無の幅で説明できる」 |
| **E-2 位置** | `m_MAM − m_SON` | **≤ −0.05** なら「帰無の位置で説明できる」 |
| **E-3 交換（★決定的）** | **r̂_MAM を SON の帰無に当てた片側 p** と **r̂_SON を MAM の帰無に当てた片側 p** | **前者 < 0.05 かつ後者 ≥ 0.05 の両方**が成り立てば「差は r ではなく帰無から来る」 |
| **E-4 機構** | **θ の年内 lag-1 自己相関**（同一年・暦日差 1 日の対のみ） | **acf1_MAM > acf1_SON** なら見立ての機構が支持される |

**E-3 が主。E-1/E-2 は E-3 の中身の分解であり、どちらか片方が支持されれば足りる**
（**幅と位置は排他ではない。両方書く**）。
**E-3 が片方しか成り立たなければ「部分的」と書く。両方とも成り立たなければ見立ては外れ。**
**E-4 は機構の裏取りであり、E-4 が外れても E-3 が通れば「差は帰無から来る。ただし
その理由は自己相関では説明できない」と書く**（**現象と機構を混ぜない**）。

## ★事前予測 H2（**通算 18 回目・確信は中**）

**E-3 は両方とも成り立つ**（＝見立て支持）。
**理由**：r の差が 0.059 しかないのに p が一桁違う以上、季節ごとの参照分布が違うほかない。
**外れうる点**：`r̂_MAM = −0.224` を SON の帰無に当てたときの p が 0.05 を跨ぐかは自明でない
（SON の帰無は `−0.283` で p=0.0275 を返しており、**0.059 手前で 0.05 を越える余地がある**）。

## 自己点検（**診断の前に走らせる**）

**保存した帰無配列から p を数え直し、`shift_season_p` が返した p と一致するか**
（**一致しなければ診断は読まない**）。**旗140 の欠陥 #67 と同型の事故を避けるため、
帰無分布は新しい実装で作り直さず、判定に使ったのと同じ関数から取り出す。**

## 追補 E の結果（**旗143・2026-09-09・`--nulldiag` 2000 本・種は旗142 と同一**）

**回帰確認**：`r̂` と `p` は旗142 と一桁まで同一（MAM −0.224 / 0.3738・SON −0.283 / 0.0275）。
**自己点検**（保存した帰無から p を数え直す）は 4 本とも一致。

| 季節 | 観測 r̂ | 片側 p | 帰無 中央値 | 帰無 SD | 標準化位置 z |
|---|---|---|---|---|---|
| **MAM** | −0.224 | 0.3738 | **−0.188** | 0.094 | **−0.38** |
| **SON** | −0.283 | 0.0275 | **−0.129** | 0.096 | **−1.60** |

| | 得た値 | 要求 | |
|---|---|---|---|
| **E-1 幅** | s_MAM/s_SON = **0.97** | ≥ 1.30 | **不支持** |
| **E-2 位置** | m_MAM − m_SON = **−0.059** | ≤ −0.05 | **支持** |
| **E-3 交換（主）** | p(r̂_MAM｜SON 帰無)=**0.1719**（要 <0.05）／p(r̂_SON｜MAM 帰無)=**0.2384**（要 ≥0.05） | 両方 | **部分的**（条件②のみ） |
| **E-4 機構** | acf1 MAM **+0.976** > SON **+0.962** | 不等号のみ | 支持（**だが差 0.014 は機構になりえない**） |
| **E-5 記録** | 連続 7 行＝暦で **MAM 9.0 日・SON 13.0 日** | — | 合否に使わない |

**★事前予測 H2（E-3 が両方成立）は外れ**（通算 **18 回中 9 勝**）。
**旗142 の見立て（MAM のほうが帰無が広い）は誤り**——**幅はほぼ同じ（0.094 対 0.096）で、
違うのは中央値である。しかも中央値は観測 r と逆向きに動く**
（r は SON が 0.059 negative 側・帰無中央値は MAM が 0.059 negative 側）。

**E-4 の書き方は失敗だった**：**大きさの要求を書かずに不等号だけを条件にしたので、
0.014 の差でも「支持」になった。＝コイン投げが半分通る規則である**（設計の穴 #70）。

**この結果は旗142 の判定（○弱い証拠）も予測 H1 の勝敗（外れ）も変えない。**
