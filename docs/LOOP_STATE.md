# LOOP_STATE — 再開ポインタ（毎周の最後に上書きされる唯一の真実）

> **このファイルの役割**: コンテキストを完全に失った Claude が、**このファイルだけ読めば作業を再開できる**ようにする。
> 会話履歴に依存する情報を一切書かないこと。「さっきの」「前述の」は禁止。すべて絶対参照（ファイル名・行番号・ID）で書く。

---

## 現在の状態

| 項目 | 値 |
|---|---|
| **最終更新** | 2026-09-01 |
| **更新した周** | #0（初期化。まだ 1 周も回っていない） |
| **作業ブランチ** | `claude/ai-continuous-execution-6062zj` |
| **最終コミット** | （初期化時点） |
| **直前の周でやったこと** | なし（セットアップのみ） |
| **次に取るべき項目** | `docs/LOOP_QUEUE.md` の `Q01` |
| **ループの停止理由** | 未起動 |

## 進行中の作業（中断された場合のみ記入）

```
なし
```

> 中断時はここに「どのファイルのどこまで書いたか」を**行番号つきで**書く。
> 例: 「`analysis_A_v15.py` を 120 行目まで執筆。`in_band()` の定義は完了、呼び出し側の置換が未了（旧 `in_band` 呼び出しが 3 箇所残っている: L188, L241, L303）」

## 今の周で得た、まだファイルに書いていない情報

```
なし
```

> **原則としてここは常に空であるべき。** 得た情報は即座に該当ファイルへ書き出すこと。
> ここに何か書いてある状態でコンテキストが飛んだら、その情報は失われる。

---

## 更新ルール（毎周の最後に必ず実行）

1. 上の表の 6 項目をすべて更新
2. 「進行中の作業」を空にする（周を完結させてから終わること）
3. 「まだファイルに書いていない情報」を空にする
4. `git add -A && git commit && git push -u origin <branch>`

**このファイルの更新とコミットを済ませていない状態で周を終えてはいけない。**
